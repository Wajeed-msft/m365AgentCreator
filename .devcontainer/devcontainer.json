{
  "name": "M365 Agent Builder",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install xdg-utils && npm install -g @microsoft/m365agentstoolkit-cli @microsoft/teams-app-test-tool && atk new -c basic-custom-engine-agent -l typescript -n myagent -i false && cd myagent && npm install @anthropic-ai/claude-code dotenv && npm install"
}

